import { createFileRoute } from '@tanstack/react-router';import { CreatorDashboard } from '@/components/CreatorDashboard';export const Route=createFileRoute('/creator/')({component:CreatorDashboard})
