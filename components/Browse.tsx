import { ArrowRight, Radio, Sparkles } from "lucide-react";
import { rooms } from "@/lib/rooms";
import { SiteHeader } from "./SiteHeader";

export function Browse() {
  const featured = rooms[0];
  return <main className="browsePage"><SiteHeader/><div className="browseShell"><aside className="sideRail"><h3>LIVE CHANNELS</h3>{rooms.slice(0,5).map(room=><a href={`/live/${room.slug}`} key={room.slug}><span className={`miniAvatar ${room.accent}`}>{room.initials}</span><span><b>{room.creator}</b><small>{room.category}</small></span><em><i/>{room.viewers}</em></a>)}</aside><section className="browseContent"><div className="welcome"><span><Sparkles size={14}/> YOUR LIVE PLACE</span><h1>What&apos;s happening<br/><em>right now.</em></h1><p>Drop into a room, stay for the people.</p></div><a href={`/live/${featured.slug}`} className={`featureRoom art-${featured.accent}`}><div className="featureCopy"><span className="livePill"><i/> LIVE</span><h2>{featured.title}</h2><p>{featured.description}</p><div><span className="channelAvatar">{featured.initials}</span><b>{featured.creator}</b><small>{featured.category} · {featured.viewers} watching</small></div></div><span className="watchNow">Watch now <ArrowRight size={18}/></span></a><div className="sectionTitle"><div><span><Radio size={14}/> LIVE NOW</span><h2>Rooms you might like</h2></div><small>{rooms.length} rooms live</small></div><div className="roomGrid">{rooms.slice(1).map(room=><a href={`/live/${room.slug}`} className="roomTile" key={room.slug}><div className={`roomArt art-${room.accent}`}><span className="livePill"><i/> LIVE</span><span className="viewerCount">{room.viewers} viewers</span><div className="artMonogram">{room.initials}</div></div><div className="roomInfo"><span className={`miniAvatar ${room.accent}`}>{room.initials}</span><div><h3>{room.title}</h3><p>{room.creator}</p><small>{room.category}</small></div></div></a>)}</div></section></div><footer><span>© 2026 uc8Live</span><span>Real people. Right now.</span><span>Community guidelines · Privacy</span></footer></main>;
}
