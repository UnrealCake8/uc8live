import { useEffect,useState } from 'react';import { authHeaders } from '@/client/auth';import { CreatorShell } from './CreatorShell';import { Video } from 'lucide-react';
export function Broadcasts(){const [items,setItems]=useState<any[]>([]);useEffect(()=>{authHeaders().then(headers=>fetch('/api/channels/mine',{headers}).then(r=>r.json())).then(d=>setItems(d.broadcasts??[]))},[]);async function toggle(id:string,value:boolean){await fetch(`/api/broadcasts/${id}`,{method:'PUT',headers:{'content-type':'application/json',...await authHeaders()},body:JSON.stringify({isPublic:value})});setItems(v=>v.map(x=>x.id===id?{...x,is_replay_public:value}:x))}return <CreatorShell><div className="pageTitle"><div><span className="eyebrow">LIBRARY</span><h1>Previous broadcasts</h1></div></div>{items.length?<div className="broadcastList">{items.map(item=><article key={item.id}><div className="miniThumb"><Video/> {item.status}</div><div><h3>{item.title}</h3><p>{new Date(item.started_at).toLocaleString()} {item.duration_seconds?`· ${Math.round(item.duration_seconds/60)} min`:''}</p></div><label className="toggle"><input type="checkbox" checked={item.is_replay_public} disabled={item.status!=='ready'} onChange={e=>toggle(item.id,e.target.checked)}/> Public replay</label></article>)}</div>:<div className="empty"><Video/><h2>No broadcasts yet</h2><p>Completed Mux recordings will appear here automatically.</p></div>}</CreatorShell>}
