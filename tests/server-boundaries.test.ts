import {describe,expect,it} from 'vitest';import fs from 'node:fs';import path from 'node:path'
function files(dir:string):string[]{return fs.readdirSync(dir,{withFileTypes:true}).flatMap(e=>e.isDirectory()?files(path.join(dir,e.name)):[path.join(dir,e.name)])}
describe('server-only boundaries',()=>{it('never imports Mux management SDK from browser components',()=>{for(const file of files('src/components'))expect(fs.readFileSync(file,'utf8')).not.toContain('@mux/mux-node')});it('retrieves and regenerates keys only after owner and freshness checks',()=>{const source=fs.readFileSync('src/server/channels/channel-service.ts','utf8');expect(source.match(/Recent authentication required/g)?.length).toBe(2);expect(source).toContain('resetMuxStreamKey')});it('enforces creator role before channel creation',()=>{expect(fs.readFileSync('src/routes/api/channels/index.ts','utf8')).toContain("requireRole('creator')")})})
