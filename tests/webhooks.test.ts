import {describe,expect,it} from 'vitest';import fs from 'node:fs'
describe('Mux webhook boundary',()=>{const route=fs.readFileSync('src/routes/api/webhooks/mux.ts','utf8');const service=fs.readFileSync('src/server/mux/webhooks.ts','utf8');it('rejects missing signatures',()=>expect(route).toContain("status:401"));it('uses official SDK signature verification',()=>expect(service).toContain('webhooks.unwrap'));it('is idempotent by unique event ID',()=>{expect(service).toContain("insertError?.code==='23505'");expect(service).toContain('duplicate:true')});it('maps verified stream lifecycle events',()=>{expect(service).toContain("'video.live_stream.active':'live'");expect(service).toContain("'video.live_stream.idle':'offline'");expect(service).toContain("'video.live_stream.disconnected':'reconnecting'")})})
