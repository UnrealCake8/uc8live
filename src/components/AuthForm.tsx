import { useState, type FormEvent } from 'react'
import { supabaseBrowser } from '@/client/auth'
import { Link, useNavigate } from '@tanstack/react-router'
import { LockKeyhole } from 'lucide-react'
export function AuthForm({register=false}:{register?:boolean}){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [name,setName]=useState('');const [username,setUsername]=useState('');const [error,setError]=useState('');const [loading,setLoading]=useState(false);const navigate=useNavigate();async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError('');const s=await supabaseBrowser();if(register){const {error}=await s.auth.signUp({email,password,options:{data:{display_name:name,username}}});if(error)setError(error.message);else navigate({to:'/creator'})}else{const {error}=await s.auth.signInWithPassword({email,password});if(error)setError('Email or password is incorrect.');else navigate({to:'/creator'})}setLoading(false)}async function google(){const s=await supabaseBrowser();await s.auth.signInWithOAuth({provider:'google',options:{redirectTo:`${location.origin}/creator`}})}return <main className="authPage"><form className="authCard" onSubmit={submit}><span className="roundIcon"><LockKeyhole/></span><span className="eyebrow">CREATOR ACCESS</span><h1>{register?'Create your account':'Welcome back'}</h1><p>{register?'Start a channel with no application required.':'Sign in to manage your channel and broadcasts.'}</p>{register&&<><label>Display name<input required value={name} onChange={e=>setName(e.target.value)} /></label><label>Username<input required pattern="[a-z0-9-]+" value={username} onChange={e=>setUsername(e.target.value.toLowerCase().replace(/[^a-z0-9-]/g,''))}/></label></>}<label>Email<input type="email" autoComplete="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input type="password" minLength={8} autoComplete={register?'new-password':'current-password'} required value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<div className="error">{error}</div>}<button className="primary" disabled={loading}>{loading?'Please wait…':register?'Create account':'Sign in'}</button><button type="button" className="secondary full" onClick={google}>Continue with Google</button><small>{register?'Already registered?':'New to uc8Live?'} <Link to={register?'/login':'/register'}>{register?'Sign in':'Create an account'}</Link></small></form></main>}
