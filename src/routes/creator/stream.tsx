import { createFileRoute } from '@tanstack/react-router';import { StreamSettings } from '@/components/StreamSettings';export const Route=createFileRoute('/creator/stream')({component:StreamSettings})
