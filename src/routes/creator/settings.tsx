import { createFileRoute } from '@tanstack/react-router';import { CreatorShell } from '@/components/CreatorShell';export const Route=createFileRoute('/creator/settings')({component:()=> <CreatorShell><div className="pageTitle"><div><span className="eyebrow">ACCOUNT</span><h1>Creator settings</h1><p>Manage your profile through Supabase authentication.</p></div></div><div className="notice">Profile editing and security controls use your authenticated Supabase account. Stream secrets are managed separately in Stream Settings.</div></CreatorShell>})
