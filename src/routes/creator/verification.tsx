import { createFileRoute } from '@tanstack/react-router'
import { IdentityVerification } from '@/components/IdentityVerification'
export const Route=createFileRoute('/creator/verification')({component:IdentityVerification})
